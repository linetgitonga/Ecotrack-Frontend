# Template for .env.dev / .env.staging / .env.prod (copy & fill; those are git-ignored).
# In CI each file is written from a GitHub Secret (ENV_DEV / ENV_STAGING / ENV_PROD).
# Keys only here — never commit real values.

APP_NAME=
API_BASE_URL=
SENTRY_DSN=
ENABLE_LOGGING=false
