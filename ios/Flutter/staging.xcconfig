// Included by the "Debug-staging" / "Release-staging" / "Profile-staging" build configs.
#include "Generated.xcconfig"

APP_DISPLAY_NAME = EcoTrack Staging
PRODUCT_BUNDLE_IDENTIFIER = com.ecotrack.app.staging
